信号量属于**睡眠锁**，获取失败进程休眠；
disable_irq禁止中断，request_irq申请中断。


- （1）**同步等待行为不同**
    `disable_irq(irq)`：屏蔽指定中断后，**阻塞等待当前正在执行的中断处理函数完全退出**才返回，做同步等待；
    `disable_irq_nosync(irq)`：**不会等待正在运行的中断处理程序**，调用后立即返回，无同步等待。
- **调用上下文限制不同**
    `disable_irq` 不能在**该中断自身的中断处理函数**中调用，否则会永久死锁；
    `disable_irq_nosync` 允许在中断上下文内部安全调用，不会死锁。
- **并发风险差异**
    `disable_irq` 能保证返回后无该中断处理程序在运行，无并发竞争；
    `disable_irq_nosync` 返回后仍可能存在正在执行的中断处理函数，代码需要自行处理竞态问题。
    （2）- **硬中断上下文有严格限制**：中断处理函数运行在硬中断上下文，**禁止睡眠、阻塞、延时、IO 操作**，按键消抖、数据上报等耗时逻辑无法直接在 ISR 中执行。
- **工作队列运行在进程上下文**：由内核 worker 线程执行任务，允许调用`msleep`、信号量、内存分配、用户空间拷贝等阻塞接口，适配按键消抖等延时需求。
- **缩短顶半部执行时间**：ISR 只做屏蔽中断、入队工作两件极简操作，快速退出硬中断上下文，减少关中断时长，提升系统实时性、避免丢失硬件中断。
    （3）
    该返回值向内核表明：**本次中断是当前设备触发，且驱动已完成本设备对应的中断顶层处理**