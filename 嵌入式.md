信号量属于**睡眠锁**，获取失败进程休眠；
disable_irq禁止中断，request_irq申请中断。


#include <linux/module.h> 
#include <linux/timer.h> 
#include <linux/jiffies.h>
static void timer_handler(unsigned long arg) 
{ printk("Timer handler called\n"); 
// 重新设置超时时间，实现周期性定时 
mod_timer(&my_timer, jiffies + TIMER_PERIOD); }
static int __init timer_drv_init(void) 
{ 
// 新式API初始化定时器，绑定处理函数与参数 timer_setup(&my_timer, timer_handler, 0); 
// 设置首次超时时刻并启动定时器
mod_timer(&my_timer, jiffies + TIMER_PERIOD); 
printk("Timer module loaded, timer started(1s period)\n"); return 0; }
static void __exit timer_drv_exit(void) 
{ // del_timer_sync同步删除，等待正在执行的定时器回调完成再返回，避免竞态崩溃 
del_timer_sync(&my_timer); 
printk("Timer module unloaded, timer deleted\n"); }
module_init(timer_drv_init); module_exit(timer_drv_exit); MODULE_LICENSE("GPL"); MODULE_DESCRIPTION("1s periodic kernel timer demo");
#define TIMER_PERIOD(1* HZ)
TIME_PERIOD(1*HZ)


向下统一硬件驱动接口
向上统一应用访问接口
中间核心层调度