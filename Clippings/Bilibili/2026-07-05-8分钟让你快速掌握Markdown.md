---
title: "8分钟让你快速掌握Markdown"
url: "https://www.bilibili.com/video/BV1JA411h7Gw/"
bvid: "BV1JA411h7Gw"
cid: "171385214"
author: "请叫我AXin"
upload_date: "2020-03-30"
subtitle_lang: "中文（中国）"
created: "2026-07-05"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=327623069&bvid=BV1JA411h7Gw&cid=171385214&page=1&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

Markdown 是一种轻量级标记语言，它允许人们使用易读易写的纯文本格式编写文档。使用Markdown 编写的文档可以轻松地导出为 HTML、Word、图像、PDF、Epub 等多种格式的文档。当前许多网站都支持使用 Markdown 来撰写文档或者发表文章。例如：GitHub、简书、StackOverflow、有道云笔记等等。

## 字幕

`00:01` Hello 大家好 我是AXin
`00:02` markdown是一种轻量级标记语言
`00:04` 允许人们非常快速地编写文档
`00:07` 使用markdown编写的文档呢
`00:09` 可以非常容易导出成html、word、图像
`00:11` PDF
`00:12` 或者epub等多种格式的文档
`00:14` 呃Markdown的后缀名呢？是点md啊
`00:19` 很多网站都支持Markdown来编写文档或者发表文章
`00:22` 比如说GitHub、简书、StackOverflow
`00:25` 有道云笔记等等
`00:26` 本视频呢我使用一个叫 stackedit 的谷歌浏览器应用
`00:31` 来向大家快速介绍Markdown语法
`00:33` 然后各个平台都有非常多的markdown编辑器
`00:36` 我会在下面放上链接
`00:37` 大家可以选择适合自己的编辑器
`00:40` 然后一个markdown文档的元素分为两类啊
`00:44` 一种是块级元素
`00:46` 块级元素呢
`00:47` 他至少占据一行
`00:48` 他也可以占据多行
`00:50` 行内元素呢他只占据自身所需要的空间
`00:55` 他可以如果不不满一行
`00:57` 后面还可以占据
`00:58` 还可以有其他的元素
`01:00` 我们先来看块级元素
`01:02` 首先我们来看标题
`01:04` 标题呢就是和word里面的标题类似
`01:07` 他有六级
`01:09` 比如说标题1
`01:13` 标题二
`01:18` 标题三等等
`01:23` 第二个是引用
`01:24` 引用就是引用一段话
`01:26` 他使用一个大于符号表示
`01:29` 我们可以看到引用的它前面有个这个竖杠
`01:32` 这是一段引用
`01:33` 接着是列表，列表分为有序列表和无序列表
`01:36` 我们先来看有序列表
`01:38` 把大象放进冰箱
`01:40` 第一步打开冰箱
`01:41` 第二部把大象塞进去
`01:47` 第三步关上冰箱
`01:49` 然后呢
`01:50` 我们看无序列表
`01:52` 使用短横线
`01:53` 那么这就是无序列表
`01:56` 你也可以使用星号
`01:59` 然后我们来再来看任务列表
`02:01` 明天要做的事 横杠
`02:03` 方括号
`02:04` 空格
`02:04` 吃饭睡觉打豆豆
`02:09` 然后呢
`02:10` 在空格里面
`02:11` 我们可以输入一个字母x
`02:12` 那么他就会勾选上
`02:14` 当然你也可以在这里勾线上
`02:15` 接着是代码块
`02:20` 三个反引号
`02:21` 加上你要输入的代码的语言
`02:24` 比如说C语言
`02:28` 数学公式
`02:30` 数学公式使用四个美元符号
`02:33` 把公式括起来
`02:34` 数学公式使用latex
`02:36` 比如说我们写一个偏导数
`02:42` 表格分为三部分表头
`02:46` 对齐方式
`02:48` 如果冒号在左边表示左对齐
`02:50` 这里的短横线至少有一个
`02:53` 如果冒号在右边表示右对齐
`02:56` 如果两边都有冒号
`02:57` 表示居中对齐好
`03:01` 我们来看脚注 脚注分两部分
`03:04` 一部分是角柱的链接
`03:05` 另一部分是角柱的内容
`03:07` 比如说一键三连
`03:08` 然后我们输入一个方扩号
`03:11` 然后一个帽子符号
`03:13` 然后输入脚注的名字
`03:15` 比如说三连
`03:16` 然后呢
`03:17` 我们在其他部分
`03:19` 比如说文章的结尾把这个三连解释一下
`03:22` 点赞、投币、收藏
`03:26` 最好再关注一下
`03:28` 那么这个就是脚注 如果我们点击这里
`03:30` 它就会跳到这个解释这里来
`03:33` 然后是横线
`03:34` 如果你想把一篇文章分割成几部分
`03:37` 我们可以使用横线
`03:37` 我们可以使用横线
`03:38` 比如说在这里输入三根横线
`03:40` 那么这边就多出了一个横线
`03:43` 这个横线是文章自动为这个脚注生成的
`03:46` 这个横线是我们自己编写的
`03:48` 比如说哈哈哈
`03:49` 这上面也是横线接着呢
`03:51` 我们来看行内元素
`03:53` 首先我们来看链接链接呢
`03:54` 使用方括号
`03:55` 然后里面输入链接的文本
`03:57` 然后呢？输入链接的地址括号是英文括号
`04:01` baidu.com那么这个就是链接
`04:03` 然后呢我们可以给他添加一个标题
`04:08` 一个搜索引擎
`04:09` 那么我们这里鼠标悬浮的时候
`04:11` 它会显示一个提示
`04:13` 然后除此之外我们还可以使用引用链接 引用链接呢
`04:17` 适用用于多次出现同一个链接的情况
`04:20` 比如说我们在文章中多次出现在百度这个链接
`04:23` 如果我们每次都把这个复制一下的话
`04:26` 那么如果我们要更改的话
`04:27` 那么就每个都需要更改
`04:29` 我们可以使用引用链接
`04:31` 引用链接的格式呢 和这个很类似
`04:33` 我们把这个复制一下
`04:35` 粘贴一下
`04:35` 然后呢
`04:36` 我们这里把它剪切掉
`04:38` 我们这里输入还是方括号ID
`04:40` 我们在后面的某个地方输入ID
`04:43` 再输入个冒号
`04:44` baidu.com 一个搜索引擎
`04:46` 我们点击同样是可以跳转到百度的
`04:49` 这样的话
`04:50` 如果我们在多个地方都想要这个链接
`04:53` 我们只需要复制百度ID再沾粘贴即可
`04:57` 如果我们需要更改
`04:58` 所有这个链接
`05:00` 只需要更改这一处就可以了
`05:02` 比如说把百度变成Google.com
`05:05` 即可
`05:06` 那么我们点击其他链接的时候
`05:08` 他将会跳转到谷歌
`05:10` 我们也可以将链接设置到标题
`05:13` 比如说请参考标题一
`05:15` 那么这里的链接
`05:16` 我们需要使用一个井号
`05:18` 加上标题一
`05:19` 那么我们点击标题的时候
`05:23` 它就会跳转到标题1
`05:24` 所以这里链接的这个名字一定要和标题的名
`05:28` 字完全一样
`05:29` 我们可以输入URL可以直接输入标准的url
`05:32` 比如说http://www.baidu.com
`05:35` 如果你输入了http
`05:37` 也就说这个协议
`05:38` 然后加上域名的话
`05:40` 他会自动往本为你生成链接
`05:42` 接下来我们来看图片图片呢和链接差不多
`05:46` 他只是多了一个感叹号
`05:48` 然后是方括号方括号内部是替代地文本
`05:51` 比如说百度
`05:53` 然后呢
`05:53` 后面是图片的链接
`05:55` 比如说
`05:55` 这是百度的搜索引擎上面的图片和链接
`05:59` 我们也可以给它加上一个说明的文字
`06:02` 比如说百度搜索那么鼠标放上来的时候他会出现一个百度搜索
`06:07` 如果你的（口误）图片是在本地的话
`06:10` 你这里可以使用你本地图片的相对地址
`06:13` 或者说绝对地址
`06:14` 然后我们来看一下其他的一些行内的一些格式
`06:18` 我们使用一对星号
`06:19` 表示强调
`06:20` 也就是说斜体使用
`06:21` 两队星号表示加粗
`06:23` 使用一对反引号
`06:24` 编写行内代码
`06:26` 比如说printf这个函数
`06:27` 下划线比较特殊
`06:28` 我们需要使用HTML的u标签下滑线
`06:31` 我们还可以使用标准的表情符号
`06:34` 表情符号是一个冒号加上表情的名称
`06:37` 比如说smile那么它会产生一个表情 表情的名称
`06:41` 可以参考emoji国际通用表情代码 视频下方我会放相关的链接
`06:46` 然后是行内数学公式
`06:48` 行内数学公式使用一对美元符号比如说
`06:51` θ等于x的平方就是行内数学公式
`06:55` 我们再来看下标下标呢？使用一对波浪号包裹起来
`07:00` 比如说H2O H2O是水的化学式
`07:04` 对吧？
`07:04` 我们想把这个二变成下标
`07:06` 那么我们用一对波浪号把它标记起来
`07:09` 也可以使用上标
`07:10` 比如说x的平方
`07:12` 我们使用一对尖帽号
`07:13` 把这个标记起来
`07:15` 然后我们还可以对文字进行高亮高亮呢
`07:18` 使用两对等号
`07:20` 这是一段高亮文字
`07:22` 这些呢就是markdown的基本的语法
`07:24` 在markdown中
`07:25` 你完全可以使用html
`07:27` 实现Mark down不支持的内容
`07:29` 例如视频等等
`07:30` 你甚至可以嵌入B站看的播放器
`07:32` 比如说打开一个视频
`07:34` 然后我们点击分享
`07:35` 这里有个嵌入代码
`07:37` 我们点击复制
`07:38` 然后再回到markdown我们进行粘贴
`07:40` 那么这里呢
`07:41` 就显示了一个视频
`07:42` 我们甚至可以在这里进行播放
`07:45` 然后最后一点
`07:46` 有些markdown编辑器可能不支持
`07:48` 今天这个视频中所提到的一部分功能
`07:51` 需要你挑选适合自己的markdown编辑器
`07:54` 如果大家喜欢我的视频
`07:55` 呃
`07:55` 希望你能一键三连加关注
`07:57` 我会做出更好的视频