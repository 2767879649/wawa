# 3.数据(数据类型)
## 基本数据类型  P 29
- 整型：字符，短整型，整型，长整型(均含有有符号，无符号)
- 浮点型，指针，聚合类型(数组，结构体，字符串）


# 4.语句 
空语句：`；`
表达式语句 `x=y+3;`
代码块 `{  }`
if 
while 
for
do
switch：default 子句
goto: `goto 语句标签` 运行跳转到标签位置

# 5.操作符和表达式
# 8.数组
二维数组作为函数参数 int A[]n]或 int (****A)[10]

adf
