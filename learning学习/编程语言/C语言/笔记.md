1. 数组直接使用，传递的是一个地址；
2. malloc(大小)在堆上申请内存，free()释放内存，堆上的内存使用指针来访问，
3. calloc(n,sizeof())会对申请空间进行初始化，malloc 不会，realloc(p,sizeof())可以对空间进行扩大或缩小
4. 随机数 ![[Pasted image 20260802171104.png]]
5. 大写字母转为小写字母：tolower()，需要#include <ctype.h>头文件
```c
if (ch >= 'A' && ch <= 'Z')//大写字母转换为小写字母
    ch = ch + 'a'- 'A';    //大小字母相差32
```
6. 在涉及到 `const char*` 的地方，一律使用 `const char*` 类型的变量去接，不要去掉 `const`。所以要少用 const
7. strstr(string，"sdf")用来查找 string 里有没有“sdf”；