# LEDC 通道
1. ledcSetup(通道，频率，分辨率)；//设置通道
2. ledcAttachPin(引脚，通道)；//将引脚和通道绑定
3. ledcWrite（通道，i）；更改 PWM 输出

# ADC
1. analogRead(引脚)                       //ADC 读取

# 串口
1. Serial.begin(115200);           // 设置波特率
2.   Serial.printf("输出值:%d\n", ADC);        //串口打印
3. Serial.print(ADC)；             //打印数字