`cJSON *root = cJSON_CreateObject();`               // 创建一个新的JSON对象
`cJSON_AddStringToObject(root,"name","xiaoming")` //添加字符串键值
`cJSON_AddNumberToObject(root, "age", 30);` //添加数字键值
`char *json_string = cJSON_Print(root);` //将 JSON 对象转换为字符串
`cJSON_Delete(root)`  //释放 JSON 对象占用的内存
`cJSON_free(json_string)` //释放 cJSON_Print 分配的内存