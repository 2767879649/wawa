1. ESP 8266 上电乱码，<font color="#9bbb59">解决：</font>重新烧录了固件
2. ESP 8266 的 AT 指令发送，<font color="#9bbb59">解决：</font>本来是发送指令进行死等，后面改进后使用状态机发送指令
3. 联网获取天气，cjson 数据解析失败，<font color="#9bbb59">解决：</font>接收缓存区不能有其他信息，在接收解析数据前清空缓存区
4. 使用 cjson 进行解析时，解析失败，<font color="#9bbb59">解决：</font>内存要大点，或者将 cJSON 解析重定向到 FreeRTOS 的堆中
5. 通过按键调节时间，并切换为天气模式(<font color="#9bbb59">实现</font>)
6. cjson 数据解析失败，<font color="#9bbb59">原因：</font>需要末尾加“\0 结束符”，还有大概率内存不够，重定向到 FreeRTOS 的堆后解决
7. <mark style="background:#ff4d4f">问题</mark>：1.  ESP 8266 初始化断电后重复执行
8. ADC 不能在中断中等待，<font color="#9bbb59">解决：</font>因为等待是阻塞的，导致定时器中断卡死