### 跨文件声明变量
extern 
#### 按键
默认为 1，按下为 0
#### 光感传感器
低电平为 1，高电平为 0
负极拔出为 1，插入为 0
#### stm 32 F 10 c 8 t 6 
- Flash 是 64 K，

#### 寄存器
是芯片内部速度最快的极小存储单元，直接集成在CPU内核里，CPU运算时临时存放数据、控制硬件，读写速度远超内存、Flash。

TIM_OCStructInit(&TIM_OCInitStructure);//默认赋初始值

GPIO_SetBits(GPIOA,GPIO_Pin_0);
