# 坑
1. 只有heap_.4 c 能够使用 vtaskdeleat()销毁任务
![[Pasted image 20260825233831.png]]
2. OLED，I 2 C ，printf存在两个以上的访问函数，需要添加锁(<font color="#9bbb59">在 1 ms 的时间片中，不足以获得足够的数据</font>)
3. FreeRTOS 的老 Demo 程序，使用的是 C 89 规则，定义结构体名字必须在开启时钟前，也可以替换为 C 99 规则

# 基础
- 临界资源 ：在同一时间，只允许一个任务或中断访问的资源(*全局变量，缓冲区数组，结构体，*)