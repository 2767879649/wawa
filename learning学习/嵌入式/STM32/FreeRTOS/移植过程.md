# 移植
这 7 个文件和 portable 里的.c 文件
![[Pasted image 20260809144652.png|548]]
创建了任务但没有执行

```
// 在FreeRTOS\include\FreeRTOSConfig.h中 — 添加3个宏映射，对应freeRTOS的接口
/* FreeRTOS 中断服务函数 → CMSIS 标准名称映射 */
#define vPortSVCHandler      SVC_Handler
#define xPortPendSVHandler   PendSV_Handler
#define xPortSysTickHandler  SysTick_Handler
```