```c
    TaskHandle_t handle1;                 //定义任务权柄，定义后使用这个调动函数
    prvSetupHardware();
    LED_Init();
    printf("Hello word");
    /* Start the scheduler. */
    xTaskCreate(Task1,"Task1",100,NULL,1,NULL);    //创建执行任务
    xTaskCreate(Task2,"Task2",100,NULL,1,NULL);
    vTaskStartScheduler();                //任务开始
```