![[Pasted image 20260811131725.png]] ![[Pasted image 20260811151358.png]]
# AT使用命令
AT+RST  //重启
## STA 模式
//ESP 作为客户端，连接路由器
AT
ATE0        //回显显示
AT+CWMODE=1
AT+CWJAP="CU_3CMU","nw89rrg2"
//等待打印 WIFI GOT IP
AT+CIPMUX=1
AT+CIPSERVER=1,8080
AT+CIFSR//查看网络连接信息

## 获取时间和天气
AT+CIPSTART="TCP","api.seniverse.com",80//建立 TCP 连接
AT+CIPMODE=1     //开启透传模式
AT+CIPSEND     //开始发送数据

GET /v3/weather/now.json?key=SgCh-_e58GLicOBpJ&location=beijing HTTP/1.1
Host: api.seniverse.com
Connection: close\r\n\r\n\x1a        //心知天气获取实时时间

## AP 模式
//ESP 作为服务端，手机连接 ESP
AT
ATE0
AT+CWMODE=2
AT+CWSAP="ESP8266","12345678",1,4 //开启 AP 热点
AT+CIPMUX=1  //开启连接
AT+CIPSERVER=1,8080 

# 初始化
```c
static uint8_t send[400];
static uint8_t recv[1024];
static uint8_t recv_index = 0;
static uint8_t flag = 0;
static uint32_t starttime;
void ESP_Send(char *cmd, uint16_t Time)
{
  sprintf(recv, "%s\r\n", cmd);
  HAL_UART_Transmit(&huart1, (uint8_t *)recv, strlen(recv), Time);
}

void ESP_Init(void)
{
  static uint8_t flag = 0;
  while (1)
  {
    if (recv_index != flag)
    {
      flag = recv_index;
      memset(recv, 0, sizeof(recv));
    }
    if (flag == 0)
    {
      memset(recv, 0, sizeof(recv));
      ESP_Send("AT", 1000);
    }
    else if (flag == 1)
    {

      ESP_Send("AT+CWMODE=1", 1000);
    }
    else if (flag == 2)
    {
      starttime = HAL_GetTick();
      ESP_Send("AT+CWJAP=\"CU_3CMU\",\"nw89rrg2\"", 1000);
      while (1)
      {
        if (strstr((char *)recv, "\r\nOK") != NULL)
        {
          break;
        }
        else if (HAL_GetTick() - starttime > 5000)
        {
          break;
        }
      }
    }

    else if (flag == 3)
    {
      ESP_Send("AT+CIPMUX=0", 100);
      HAL_Delay(500);
    }
    else if (flag == 4)
    {
      ESP_Send("AT+CIPSTART=\"TCP\",\"api.seniverse.com\",80", 100);
      HAL_Delay(500);
    }
    else if (flag == 5)
    {
      ESP_Send("AT+CIPMODE=1", 100);
      HAL_Delay(500);
    }
    else if (flag == 6)
    {
      ESP_Send("AT+CIPSEND", 100);
      HAL_Delay(500);
    }
    else if (flag == 7)
    {
      ESP_Send("GET /v3/weather/now.json?key=SgCh-_e58GLicOBpJ&location=beijing&language=en", 500);
      HAL_Delay(1000);
      break;
    }
    OLED_ShowInt(6, 20, recv_index, 8);
    printf("%s\r\n", recv); // 或者用 HAL_UART_Transmit 发送
    HAL_Delay(1000);        // 给串口发送留点时间
    if (strstr((char *)recv, "\r\nOK") != NULL)
    {
      recv_index++;
    }
    memset(recv, 0, sizeof(recv));
    OLED_ShowString(6, 0, "      ", 8);
  }
}
/**
 * @brief 等待指定的响应字符串出现在接收缓冲区中
 * @param target: 要查找的目标字符串（如 "OK"、"CONNECT"、"HTTP/1.1"）
 * @param timeout_ms: 超时时间（毫秒）
 * @param clear_before: 是否在等待前清空接收缓冲区（1=清空，0=不清空）
 * @retval 0: 找到目标, -1: 超时
 */
// int WaitForResponse(const char *target, uint32_t timeout_ms)
// {
//   memset(recv, 0, sizeof(recv)); // 清空接收缓冲区
//   uint32_t start = HAL_GetTick();
//   while (HAL_GetTick() - start < timeout_ms)
//   {
//     // 在接收缓冲区中查找目标字符串
//     if (strstr(recv, target) != NULL)
//     {
//       flag++;
//       return 0; // 找到目标
//     }
//     HAL_Delay(10); // 等待更多数据到达
//   }
//   return -1; // 超时
// }
// void ESP_SendCmd(char *cmd, uint16_t wait_ms)
// {
//   sprintf(send, "%s\r\n", cmd);
//   HAL_UART_Transmit(&huart1, (uint8_t *)send, strlen(send), 1000);
// }
// void ESP_Init(void)
// {
//   ESP_SendCmd("AT+RST", 1000);
//   WaitForResponse("\r\nOK\r\n", 1000);
//   HAL_Delay(2000);
//   if (strstr(recv, "ready") != NULL)
//   {

//     OLED_ShowString(0, 0, "ESP8266 Ready", 8);
//     while (1)
//     {
//       if (flag == 0)
//       {
//         ESP_SendCmd("AT", 1000);
//         WaitForResponse("\r\nOK\r\n", 1000);
//       }
//       else if (flag == 1)
//       {
//         ESP_SendCmd("AT+CWMODE=1", 1000);
//         WaitForResponse("\r\nOK\r\n", 1000);
//       }
//       else if (flag == 2)
//       {
//         starttime = HAL_GetTick();
//         ESP_SendCmd("AT+CWJAP=\"CU_3CMU\",\"nw89rrg2\"", 10000);
//         WaitForResponse("\r\nOK\r\n", 10000);
//         HAL_Delay(2000);
//       }
//       else if (flag == 3)
//       {
//         ESP_SendCmd("AT+CIPSTART=\"TCP\",\"api.seniverse.com\",80", 100);
//         WaitForResponse("\r\nOK\r\n", 1000);
//       }
//       else if (flag == 4)
//       {
//         ESP_SendCmd("AT+CIPMODE=1", 100);
//         WaitForResponse("\r\nOK\r\n", 1000);
//       }
//       else if (flag == 5)
//       {
//         ESP_SendCmd("AT+CIPSEND", 100);
//         WaitForResponse("\r\nOK\r\n", 1000);
//       }
//       else if (flag == 6)
//       {
//         ESP_SendCmd("GET /v3/weather/now.json?key=SgCh-_e58GLicOBpJ&location=beijing&language=en", 500);
//         WaitForResponse("results", 1000);
//         printf(recv);
//         HAL_Delay(100);
//         break;
//         OLED_ShowString(6, 0, "      ", 8);
//       }
//       OLED_ShowInt(6, 20, flag, 8);
//       HAL_Delay(2000);
//     }
//   }
// }
```