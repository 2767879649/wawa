![[Pasted image 20260811131725.png]] ![[Pasted image 20260811151358.png]]
# AT使用命令
AT+RST  //重启
## STA 模式
//ESP 作为客户端，连接路由器
AT
ATE0
AT+CWMODE=1
AT+CWJAP="CU_3CMU","nw89rrg2"
//等待打印 WIFI GOT IP
AT+CIPMUX=1
AT+CIPSERVER=1,8080
AT+CIFSR
## AP 模式
//ESP 作为服务端，手机连接 ESP
AT
ATE0
AT+CWMODE=2
AT+CWSAP="ESP8266","12345678",1,4 //开启 AP 热点
AT+CIPMUX=1  //开启连接
AT+CIPSERVER=1,8080 

# 初始化
```c
int ESP_Send(char*cmd,char*ack,int16_t time)
{
    int i;
    for(i=5;i>0;i--)
    {
        Serial_SendString(cmd);
        Delay_ms(500);
        if(strcmp(wenbenData,ack)==0)
            break;
    }
    while(Serial_RxFlag!=1)
    {
        if((time--)<0)
            return 1;
    }
    OLED_ShowString(4,1,"                  ");
    OLED_ShowString(4,1,wenbenData);
    Serial_RxFlag=0;
    return 0;
}
void ESP_Init()        //初始化函数
{
    Serial_Init();
    Delay_ms(2000);
    if(ESP_Send("AT\r\n","OK",1000)==1)
        ESP_flag=1;
    if(ESP_Send("ATE0\r\n","OK",1000)==1)
    ESP_flag=2;
    if(ESP_Send("AT+CWMODE=1\r\n","OK",1000)==1)
    ESP_flag=3;
    if(ESP_Send("AT+CWJAP=\"CU_3CMU\",\"nw89rrg2\"\r\n","WIFI GOT IP",10000)==1)
    ESP_flag=4;
    if(ESP_Send("AT+CIPMUX=1\r\n","OK",1000)==1)
    ESP_flag=5;
    if(ESP_Send("AT+CIPSERVER=1,8080\r\n","CONNECT",5000)==1)
    ESP_flag=6;
    if(ESP_Send("AT+CIFSR\r\n","CONNECT",5000)==1)
    ESP_flag=7;
    wenbenDataclear();
}
```