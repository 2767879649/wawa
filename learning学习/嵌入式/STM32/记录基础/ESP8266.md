![[Pasted image 20260811131725.png]] ![[Pasted image 20260811151358.png]]
# AT使用命令
AT+RST  //重启
## STA 模式
//ESP 作为客户端，连接路由器
AT
ATE0
AT+CWMODE=1
AT+CWJAP="CU_3CMU","nw89rrg2"
//等待打印 WIFI GOT IP
AT+CIPMUX=1
AT+CIPSERVER=1,8080
AT+CIFSR
WIFI DISCONNECT


WIFI GOT IP
## AP 模式
//ESP 作为服务端，手机连接 ESP
AT
ATE0
AT+CWMODE=2
AT+CWSAP="ESP8266","12345678",1,4 //开启 AP 热点
AT+CIPMUX=1  //开启连接
AT+CIPSERVER=1,8080 

# 初始化
```c
int ESP_Send(char*cmd,char*ack,int16_t time)
{
    int i;
    int16_t timeout=time;
    for(i=0;i<=5;i++)
    {
        wenbenDataclear();
        time=timeout;
        Serial_SendString(cmd);
        OLED_ShowString(4,1,"                  ");
        OLED_ShowString(4,1,wenbenData);
        if(strstr(wenbenData,"ERROR")==NULL)
        {
            OLED_ShowString(4,1,"                  ");
        OLED_ShowString(4,1,wenbenData);
            Delay_ms(500);
            return 0;
        }
    }
}
    
void ESP_Init()
{
    Serial_Init();
    Delay_ms(2000);
    ESP_Send("AT\r\n","OK",1000);
    ESP_Send("ATE0\r\n","OK",1000);
    ESP_Send("AT+CWMODE=1\r\n","OK",1000);
    ESP_Send("AT+CIPMUX=1\r\n","OK",1000);
    ESP_Send("AT+CWJAP=\"CU_3CMU\",\"nw89rrg2\"\r\n","WIFI GOT IP",30000);
    while(1)
    {
        if(strstr(wenbenData,"OK")!=NULL)
        {
        ESP_Send("AT+CIPSERVER=1,8080\r\n","OK",5000);
            break;
        }
        OLED_ShowString(4,1,"              ");
        Delay_ms(100);
        OLED_ShowString(4,1,"buzy...");
    }
    if(ESP_flag==0)
    {
    OLED_ShowString(4,1,"              ");
        OLED_ShowString(4,1,"COMEND");
    }
}
```