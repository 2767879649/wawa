# MPU6050
**1. 产品名称**
- **MPU6050**（6轴姿态传感器）
**2. 核心功能**
- 测量芯片自身 **X、Y、Z轴** 的**加速度**和**角速度**参数。
- 通过**数据融合**，可进一步得到**姿态角**。
**3. 内部组成（两大核心部件）**
- **3轴加速度计**：测量X、Y、Z轴的加速度。
- **3轴陀螺仪传感器**：测量X、Y、Z轴的角速度。
**4. 典型应用场景**
- 平衡车、飞行器等需要检测自身姿态的场景。
## MPU6050参数
- 16位ADC采集传感器的模拟信号，量化范围：-32768~32767
- 加速度计满量程选择：±2、±4、±8、±16（g）
- 陀螺仪满量程选择：±250、±500、±1000、±2000（°/sec）
- 可配置的数字低通滤波器
- 可配置的时钟源
- I2C从机地址：1101000（AD0=0）
    - 1101001（AD0=1）

# 使用
使用前需要先解除睡眠
```c
void MPU6050_Init()
{
    MyI2C_Init();
    MPU6050_WriteReg(MPU6050_PWR_MGMT_1, 0x01);        //电源管理寄存器1，取消睡眠模式，选择时钟源为X轴陀螺仪
    MPU6050_WriteReg(MPU6050_PWR_MGMT_2, 0x00);        //电源管理寄存器2，保持默认值0，所有轴均不待机
    MPU6050_WriteReg(MPU6050_SMPLRT_DIV, 0x09);        //采样率分频寄存器，配置采样率
    MPU6050_WriteReg(MPU6050_CONFIG, 0x06);            //配置寄存器，配置DLPF
    MPU6050_WriteReg(MPU6050_GYRO_CONFIG, 0x18);    //陀螺仪配置寄存器，选择满量程为±2000°/s
    MPU6050_WriteReg(MPU6050_ACCEL_CONFIG, 0x18);
    //MPU6050_WriteReg(设备寄存器地址, 0x18);，上面的是宏定义
}//MPU配置，0xxx来自手册中的功能
```
```c
void MPU6050_WriteReg(uint8_t RegAddress,uint8_t Data)
{
    MyI2C_Start();                //开始位
    MyI2C_SendByte(0xD0);        //找到模块设备地址
    MyI2C_ReceiveAck();            
    MyI2C_SendByte(RegAddress);        //找到功能寄存器地址
    MyI2C_ReceiveAck();
    MyI2C_SendByte(Data);            //传入数据
    MyI2C_ReceiveAck();
    MyI2C_Stop();                //停止位
}//修改寄存器功能
uint8_t MPU6050_ReadReg(uint8_t RegAddress)
{
    uint8_t Data;                
    MyI2C_Start();                //开始位
    MyI2C_SendByte(0xD0);        //找到模块设备地址
    MyI2C_ReceiveAck();
    MyI2C_SendByte(RegAddress);    ////找到功能寄存器地址
    MyI2C_ReceiveAck();
    MyI2C_Start();                //重复开始位
    MyI2C_SendByte(0xD0|0x01);    //改为读取模式
    MyI2C_ReceiveAck();
    Data=MyI2C_ReceiveByte();        //读取数据
    MyI2C_SendAck(1);            //发送响应为1，停止发送，为0，继续发送
    MyI2C_Stop();                //停止位
    return Data;
}//读取寄存器内容

```