1. 按住按钮不放，再上电，慢闪进入指令模式
2. `AT+PSWD=1234567890 `       //设置密码
3. AT+ROLE=0      //0为从机模式，1 为主机模式
4. AT+ADDR?        //获取从机地址，使用从机地址需要将冒号改为逗号
5. AT+UART=115200   //设置波特率
主机与从机之间可以连接(需要两个 HC-05 模块)
- 配对密码需要一致
- 主机需要从机的地址
- 波特率一致
- AT+CMODE=0    //主机固定模式连接