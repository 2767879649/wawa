![[Pasted image 20260722193825.png|383]]



![[Pasted image 20260722195124.png|500]]


![[Pasted image 20260722200447.png|299]]
![[Pasted image 20260722211754.png|475]]
# USART串口通信
**电平标准**
电平标准是数据1和数据0的表达方式，是传输线缆中人为规定的电压与数据的对应关系。串口常用的电平标准有如下三种：
- **TTL电平**(常用)：+3.3V或+5V表示1，0V表示0
- **RS232电平**：-3～-15V表示1，+3～+15V表示0
- **RS485电平**：两线压差+2～+6V表示1，-2～-6V表示0（差分信号）

## printf 使用
在魔术棒编辑器中添加 `--no-multibyte-chars` 就可以串口发送中文
1. printf 重定向
```
int fputc(int ch,FILE *f)
{
    Serial_SendByte(ch);
    return ch;
}//main.c中直接printf()使用，不需要声明
```
2. 发送字符串
```
void Serial_Printf(char *format, ...)
{
    char String[100];                //定义字符数组
    va_list arg;                    //定义可变参数列表数据类型的变量arg
    va_start(arg, format);            //从format开始，接收参数列表到arg变量
    vsprintf(String, format, arg);    //使用vsprintf打印格式化字符串和参数列表到字符数组中
    va_end(arg);                    //结束变量arg
    Serial_SendString(String);        //串口发送字符数组（字符串）
}
//声明后，在main.c中使用函数Serial_Printf("你好，世界",123);

```
## 初始化
```

```