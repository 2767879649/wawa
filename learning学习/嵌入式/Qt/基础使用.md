# 调试输出

`qDebug() << "要打印的字符串";`



# UI控件基本操作
ui->控件名字
- ->setText("")//设置ui显示文本
- ->setIcon(QIcon "xxxx路径");//图片
```
ui->控件对象名->setText("显示的文本");     // 设置显示文字
ui->控件对象名->setIcon(QIcon("图片路径")); // 设置图标，支持资源文件(:/xxx.png)
```