# 题库

> 自动生成的复习题库 | 来源: 2026/7/1

---

## 2026-07-01 导入 (1 题)

### Q: 在中断处理函数中，不能调用的函数是什么？
A: 在中断处理函数中，不能调用的函数是()。

---


## 2026-07-01 导入 (10 题)

### Q: Linux内核中，kthread_create函数用于创建什么？
A: 创建内核线程。

---

### Q: 在中断处理函数中，不能调用的函数是什么？
A: 不能调用的函数是可能导致睡眠或阻塞的函数，例如`kmalloc(GFP_KERNEL)`、`mutex_lock()`、`copy_from_user()`等。

---

### Q: 以下哪个函数用于禁止中断但不等待当前中断处理完成？
A: `local_irq_disable()`。

---

### Q: 关于中断嵌套，下列说法正确的是什么？
A: 中断嵌套是指高优先级中断可以打断低优先级中断的处理。

---

### Q: 以下哪个函数用于使能中断？
A: `local_irq_enable()`。

---

### Q: 信号量的down操作如果无法获取信号量，进程会怎样？
A: 进程会进入休眠状态，等待信号量可用。

---

### Q: 自旋锁适用于什么场景？
A: 适用于短时间持有锁且不允许睡眠的上下文，如中断处理函数。

---

### Q: 在Linux内核模块中，__init宏的作用是什么？
A: 标记初始化函数，在模块加载后释放其占用的内存。

---

### Q: 在Linux内核中，申请中断的函数是什么？
A: `request_irq()`。

---

### Q: 关于IO多路复用，下列说法正确的是什么？
A: IO多路复用允许单个线程监视多个文件描述符，当其中某个描述符就绪时进行相应操作。

---

