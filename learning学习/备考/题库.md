
## 2026-07-01 导入 (10 题)

### Q: Linux命令中，查询当前文件夹绝对路径的命令是什么？
A: Linux命令中，查询当前文件夹绝对路径的命令是pwd。

---

### Q: 决定Shell将到哪些目录中寻找命令的环境变量是什么？
A: 决定Shell将到哪些目录中寻找命令的环境变量是PATH。

---

### Q: 最简的I2C通信需要几根信号线（地线除外）？
A: 最简的I2C通信需要2根信号线（地线除外）。

---

### Q: 以下不是嵌入式操作系统的是？
A: 以下不是嵌入式操作系统的是Windows。

---

### Q: 用于卸载Linux内核模块的命令是什么？
A: 用于卸载Linux内核模块的命令是rmmod。

---

### Q: 在ARM处理器中，复位后CPU通常从哪个地址取第一条指令？
A: 在ARM处理器中，复位后CPU通常从0x00000000地址取第一条指令。

---

### Q: 下面关于冯·诺依曼结构描述正确的是？
A: 下面关于冯·诺依曼结构描述正确的是：程序和数据共用同一存储空间。

---

### Q: 以下不属于串行通信方式的接口是？
A: 以下不属于串行通信方式的接口是并行接口。

---

### Q: 以下哪个命令用于查看当前Linux系统中已注册的设备号信息？
A: 用于查看当前Linux系统中已注册的设备号信息的命令是cat /proc/devices。

---

### Q: Linux命令执行期间，按下键盘哪个键能终止当前运行的命令？
A: Linux命令执行期间，按下键盘Ctrl+C键能终止当前运行的命令。

---

