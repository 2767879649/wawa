
用\\实现
sudo apt update

# 快捷指令
- `ctrl+alt+t` 新建立一个终端
- `crtl+shift +q` 关闭一个终端
- `crtl+shift + ++` 放大终端
- `crtl+ --` 缩小终端
- `crtl+ alt` 鼠标退出虚拟机内部
- `crtl+ G`鼠标进入虚拟机内部
- 
---
# 代码命令
- uname -s 查看内核名称
- uname -r 查看内核版本号
- su root 切换管理员，exit 退出
- su zhu  切换普通用户
- hostname 查看计算机名称
- sudo hostnamectl set-hostname **new-name**     `修改主机名具体流程` [^1]

---

# 名称
- wa 用户名
- ubuntu 计算机名
- 

[^1]: **修改主机名流程**
    1. sudo hostnamectl set-hostname **new-name** 更改计算机名
    2. echo "127.0.1.1 new-name" | sudo tee /etc/hosts 同步hosts文件
    3. 注销重登

# Linux 命令基础

## 文件权限

|权限|符号|数值|
|---|---|---|
|可读|`r`|4|
|可写|`w`|2|
|可执行|`x`|1|

- `chmod` 通过三位数字设定权限：**用户 (u)** | **用户组 (g)** | **其他用户 (o)**
- 每位 = 三种权限数值之和，例如：
    - `755` → u=rwx(7), g=rx(5), o=rx(5) — 常见于可执行文件/目录
    - `644` → u=rw(6), g=r(4), o=r(4) — 常见于普通文件
    - `777` → 所有人可读可写可执行（不安全，避免使用）

## 基础操作

|命令|说明|
|---|---|
|`pwd`|显示当前所在路径|
|`ls`|列出当前目录内容（`ls -l` 详细信息，`ls -a` 含隐藏文件）|
|`cd ..`|返回上一级目录|
|`Tab`|自动补全命令/文件名|
|`cp 源 目标`|复制文件（`cp -r` 复制文件夹）|
|`mv 源 目标`|移动 / 重命名文件|
|`cat 文件名`|查看文件内容|
|`echo "内容"`|输出文本到终端（`echo "xxx" > file.txt` 重定向写入文件）|
|`touch 文件名`|创建空文件，或更新文件时间戳|
|`man 命令`|查看命令使用手册（如 `man ls`）|

## 目录与文件管理

```bash
mkdir 目录名          # 创建目录
mkdir -p a/b/c/d      # 嵌套创建多级目录

rm 文件名             # 删除文件
rm xx xx xx           # 可同时删除多个文件
rm -r 文件夹名        # 递归删除文件夹（危险，确认后执行）
```

## 编辑器 (gedit)

```bash
gedit                 # 打开编辑器（新窗口）
gedit xxx             # 打开指定文件
```
### C++文件编译运行
```
1. g++ xx -o xx
2. ./xx
```

| 快捷键        | 功能      |
| ---------- | ------- |
| `Ctrl + T` | 新建标签页   |
| `Ctrl + W` | 关闭当前标签页 |
