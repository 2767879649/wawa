
流程：
收到任务->去caht当顾问聊清楚需求，出计划->造产品上code->批量杂货丢给cowork->你只在关键结点点头摇头

强模型负责想，日常模型负责干

**一个小技巧**：提示词带上 **ultrathink**（如 ultrathink 帮我对比几种方案，先不写代码），Claude Code 会让模型这一轮想得更深。与 effort 的区别：effort 是会话级（一直生效），ultrathink 只管当前这条消息——日常别用，啃硬骨头那一问再加。、
**必养成的习惯：大项目先进 Plan mode。** 让 AI 列计划，你看过、改过、点头了，它才碰文件。能明显减少”它建了个完全不对的东西”这种返工。


先和AI交流做一个什么东西，具体要干什么，得到方案后在进行创建
- **先定范围**：这次只做 MVP，不要一次塞 P0/P1/P2。新手可以直接答：先做本地可用版本，收藏、标签、搜索、日报卡片先跑通，定时任务和上线以后再加。
    
- **再定数据**：收藏内容先存本地文件，不上云数据库；资讯源让它推荐 4-6 个稳定免费的 RSS，你挑完再执行
    
- **最后定外观**：有喜欢的产品风格就给截图或参考链接；没有就说：保持简单、干净、像一份排版讲究的电子报纸。
- 
- > /init 
- 生成 CLAUDE.md
- 每个项目开工时一次
- 
> update CLAUDE.md （直接打这句）
> 把进度写进记忆
> 每完成一个功能
- 
> /compact
> 压缩对话历史
> 上下文超 50% 时

> /usage
> 查额度
> 大项目动工前

> /rewind （部分版本支持）
> 回滚代码和对话到较早节点
> AI 改崩时，优先回到干净节点，而不是在混乱上下文里反复重来

- **50% 红线**：上下文用量超 50% 后，AI 更容易遗漏前面的细节
- - **先存档再压缩**：先 update CLAUDE.md 再 /compact，顺序反了容易丢进度
- - **CLAUDE.md 越短越好**：它是软约束（只是进入上下文），别写长篇大论；想要硬约束用钩子（第二部讲）
## 装备一 · 命令 Command
适合保存经常重复说的一大段话。比如每次收尾都要总结本轮做了什么，做成 /ship：
## 装备三 · 钩子 Hook

**痛点一：忘了存档。** 自动化它：

我想创建一个钩子（hook）：每当一个新功能开发完成并测试通过后，自动更新 CLAUDE.md 文件记录进度。

触发方式选 **stop hook**（每轮结束自动检查）。

**痛点二：AI 说”做完了”，其实没做完。** 装个验收员：

帮我创建一个 hook：在你准备结束任务前做交付验收。规则：如果本轮修改了代码、配置或文档，但没有测试过，或者检查发现还有待办事项没完成——不允许结束，继续工作直到验证通过。

从此每次收工前强制自查，“虚标完成”会少很多。可挂的节点还有：调用工具前后、子代理结束前、上下文压缩前后。

趁它干活想问别的？按 **Ctrl+;** 或输入 /btw 开”小窗”聊天，不打断主任务。

⚠ **每新增一个子代理，回头让小队长认识它**：

查看我现有的所有子代理和它们的职责，更新 CLAUDE.md 里的任务路由规则：什么任务委派给谁、什么任务不委派、判断原则是什么。