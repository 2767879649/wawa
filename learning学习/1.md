
流程：
收到任务->去caht当顾问聊清楚需求，出计划->造产品上code->批量杂货丢给cowork->你只在关键结点点头摇头

强模型负责想，日常模型负责干

**一个小技巧**：提示词带上 **ultrathink**（如 ultrathink 帮我对比几种方案，先不写代码），Claude Code 会让模型这一轮想得更深。与 effort 的区别：effort 是会话级（一直生效），ultrathink 只管当前这条消息——日常别用，啃硬骨头那一问再加。、
**必养成的习惯：大项目先进 Plan mode。** 让 AI 列计划，你看过、改过、点头了，它才碰文件。能明显减少”它建了个完全不对的东西”这种返工。


先和AI交流做一个什么东西，具体要干什么，得到方案后在进行创建
- **先定范围**：这次只做 MVP，不要一次塞 P0/P1/P2。新手可以直接答：先做本地可用版本，收藏、标签、搜索、日报卡片先跑通，定时任务和上线以后再加。
    
- **再定数据**：收藏内容先存本地文件，不上云数据库；资讯源让它推荐 4-6 个稳定免费的 RSS，你挑完再执行
    
- **最后定外观**：有喜欢的产品风格就给截图或参考链接；没有就说：保持简单、干净、像一份排版讲究的电子报纸。
- 
- > /init 
- 生成 CLAUDE.md
- 每个项目开工时一次
- 
> update CLAUDE.md （直接打这句）
> 把进度写进记忆
> 每完成一个功能
- 
> /compact
> 压缩对话历史
> 上下文超 50% 时

> /usage
> 查额度
> 大项目动工前

> /rewind （部分版本支持）
> 回滚代码和对话到较早节点
> AI 改崩时，优先回到干净节点，而不是在混乱上下文里反复重来

- **50% 红线**：上下文用量超 50% 后，AI 更容易遗漏前面的细节
- - **先存档再压缩**：先 update CLAUDE.md 再 /compact，顺序反了容易丢进度
- - **CLAUDE.md 越短越好**：它是软约束（只是进入上下文），别写长篇大论；想要硬约束用钩子（第二部讲）
## 装备一 · 命令 Command
适合保存经常重复说的一大段话。比如每次收尾都要总结本轮做了什么，做成 /ship：
## 装备三 · 钩子 Hook

**痛点一：忘了存档。** 自动化它：

我想创建一个钩子（hook）：每当一个新功能开发完成并测试通过后，自动更新 CLAUDE.md 文件记录进度。

触发方式选 **stop hook**（每轮结束自动检查）。

**痛点二：AI 说”做完了”，其实没做完。** 装个验收员：

帮我创建一个 hook：在你准备结束任务前做交付验收。规则：如果本轮修改了代码、配置或文档，但没有测试过，或者检查发现还有待办事项没完成——不允许结束，继续工作直到验证通过。

从此每次收工前强制自查，“虚标完成”会少很多。可挂的节点还有：调用工具前后、子代理结束前、上下文压缩前后。

趁它干活想问别的？按 **Ctrl+;** 或输入 /btw +问题，不打断主任务，并且本次提问不会存入主对话上下文，不会干扰钩子代码执行逻辑

⚠ **每新增一个子代理，回头让小队长认识它**：

查看我现有的所有子代理和它们的职责，更新 CLAUDE.md 里的任务路由规则：什么任务委派给谁、什么任务不委派、判断原则是什么。

## 装备五 · 通往外部世界：连接器 / MCP / CLI


CLI 很适合补齐具体工具能力。安装时直接说：帮我安装 XX CLI，并告诉我每一步怎么操作。值得装的：

- **Vercel CLI**：部署上线
    
- **Google Workspace CLI**：操作 Docs / Sheets / Gmail
    
- **Firecrawl CLI**：爬网页
    
- **ffmpeg / whisper**：视频剪辑 / 语音转文字，视频流水线的地基


到底该用哪个？四行判断标准

你想要

用什么

- 让 Claude 稳定完成某个流程：写 Skill
- 让 Claude 能用某个外部服务 / 工具：装 MCP / 连接器
- 让 Claude 自动响应某种事件：配 Hook
- 一站式安装某个能力组合：找 Plugin

大礼包 · Plugins：整个职位打包安装

Skill 是一条流程，**Plugin 是流程的集合体 = 一个完整职位**（skills + hooks + MCP 打包）。比如 marketing 插件，品牌对齐、活动策划、竞品分析、内容创作全配齐——把整个营销部门装进来。入口：输入框 **+** → Plugins，注意区分官方（by Anthropic）和第三方出品。

**新手第一个必装：Superpowers**——开发最佳实践包：先头脑风暴钉死需求 → 写计划 → 分批执行 → 审查。大项目开工前说一句 先用 Superpowers 头脑风暴，只梳理需求，不写代码，它会一个问题一个问题把你的模糊需求钉死。
经典配合：公众号自动发布 = Skill 管流程 + MCP 管”搜热点”这个能力。
彩蛋：你创建的所有装备都存在项目的 .claude 文件夹里，全是人话写的文本文件，随时围观、修改、复制给朋友。


别急着把本地应用原封不动公开。你的灵感日报里可能藏着这些东西：

- 本地收藏数据
    
- RSS 抓取逻辑
    
- 个人资料
    
- API Key
    
- 测试数据


---
核心一句话：**别把所有事都塞进同一个会话。** 一个项目可以拆几条线同时推进：本地会话继续改功能，云端会话处理独立小改动，Chat 负责文案和思路。
### **一号会话 · 本地 Code（改核心功能）**——回到日报的本地会话：
给灵感库加一个"导出"功能：一键把我收藏的所有灵感（含标签和日期）导出成一份排版好的 Markdown 文件，方便我整理进笔记软件。
### **二号会话 · 云端 Code（改一个独立小功能）**——新建会话，环境从 Local 切到 **Remote（远程 / 云端）**，选中 ai-daily 仓库：
给日报页面加一个深色模式开关，记住用户的选择。
它在云端干活，**合上电脑它也继续跑**。
### **三号会话 · Chat（不碰项目文件，只出文案）**——Chat 新开对话：
我刚做了一个"每日 AI 灵感日报"应用：自动抓取 AI 资讯、一键收藏打标签、生成可分享的日报卡片。我想把它发到社交平台展示。帮我写 3 条不同风格的发帖文案：一条讲故事的、一条列功能的、一条带点幽默的。

==**pandas**==

**Chat 聊需求 → Plan mode 出计划 → 回答反问 → 批准 → 预览验收 → /init 存档**