#include "main.h"
#include "i2c.h"
#include "rtc.h"
#include "usart.h"
#include "gpio.h"
#include "stdio.h"
#include "string.h"
int fputc(int ch, FILE *f)
{
  HAL_UART_Transmit(&huart2, (uint8_t *)&ch, 1, HAL_MAX_DELAY);
  return ch;
}
/* Private includes ----------------------------------------------------------*/
/* USER CODE BEGIN Includes */
/* USER CODE END Includes */

/* Private typedef -----------------------------------------------------------*/
/* USER CODE BEGIN PTD */
uint8_t recv[1024];
static uint8_t recv_index = 0;
static uint8_t jieshou[100];
void ESP_Send(char *cmd, uint16_t Time)
{
  char recv[200];
  sprintf(recv, "%s\r\n", cmd);
  HAL_UART_Transmit(&huart1, (uint8_t *)recv, strlen(recv), Time);
}
void ESP_Init(void)
{
  ESP_Send("AT", 100);
}  
HAL_Delay(200);
  ESP_Send("ATE0", 100);
  HAL_Delay(100);
  ESP_Send("AT+CWMODE=0", 100);
  HAL_Delay(100);
  ESP_Send("AT+CWJAP=\"CU_3CMU\",\"nw89rrg2\"", 100);
  HAL_Delay(2000);
  char http_request[] =
      "GET /v1/forecast?latitude=39.9042&longitude=116.4074&current_weather=true HTTP/1.1\r\n"
      "Host: api.open-meteo.com\r\n"
      "Connection: close\r\n";
  ESP_Send(http_request, 100);