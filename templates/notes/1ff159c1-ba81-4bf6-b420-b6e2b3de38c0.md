#虚拟机指令
删除sudo rm  xxx
更改名字 sudo mv xxx（旧）xxx（新）

# open
使用

cd 使用
./运行

使用软件进行连接
1. 使用
sudo cmake -D CMAKE_BUILD_TYPE=Release -D CMAKE_INSTALL_PREFIX=/usr/local -D OPENCV_EXTRA_MODULES_PATH=/home/zz/opencv/opencv-3.4.5/opencv_contrib-3.4.5/modules/ -DOPENCV_ENABLE_NONFREE=True ..
cheese摄像头
cp -r build-untitled1-A9-Debug /home/xx/driver/rootfs/加载进板子
2.save
3. 重启
